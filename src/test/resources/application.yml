spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jpashop2
    username: sa
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql : true

logging:
  level:
    org.hibernate.orm.jdbc.bind: trace